% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_isotope_expanded_table.R
\name{create_isotope_expanded_table}
\alias{create_isotope_expanded_table}
\title{Create an Expanded Table of Parent and Isotope Daughter Formulas}
\usage{
create_isotope_expanded_table(
  mfd,
  id_col = "peak_id",
  allow_duplicates = TRUE,
  elements = NULL
)
}
\arguments{
\item{mfd}{A \code{data.table} containing molecular formula information in wide
format, including isotope count columns, or a character vector of molecular
formulas. Character input is first converted with
\code{\link[=convert_molecular_formula_to_data_table]{convert_molecular_formula_to_data_table()}}.}

\item{id_col}{Name of the column in \code{mfd} used to define isotope groups.
Default is \code{"peak_id"}.}

\item{allow_duplicates}{Logical. If \code{TRUE} (default), isotope daughter formulas
are created for each input row using \code{id_col} as group identifier. If
\code{FALSE}, the result is based on unique isotope compositions only.}

\item{elements}{Optional character vector of element symbols
(matching \code{masses$symbol}) to restrict isotope expansion. If \code{NULL}
(default), all eligible elements detected in \code{mfd} are used.}
}
\value{
A \code{data.table} containing parent and daughter formulas, including
isotope annotation columns for downstream validation.
}
\description{
Creates a new molecular formula table containing the original parent formulas
and their corresponding single-isotope daughter formulas.
}
\details{
The output includes annotation columns that facilitate isotope validation in
downstream workflows:
\itemize{
\item \code{iso_role} indicates whether a row represents a \code{"parent"} or
\code{"daughter"} isotopologue.
\item \code{iso_element} stores the element symbol for which the isotope
substitution was generated (e.g. \code{"C"}, \code{"N"}, \code{"S"}).
\item \code{iso_from} and \code{iso_to} store the parent and daughter isotope labels
(e.g. \code{"12C"} and \code{"13C"}).
}
}
\seealso{
Other isotopes: 
\code{\link{calc_isotope_pattern}()},
\code{\link{eval_isotopes}()},
\code{\link{uplot_isotope_precision}()}
}
\concept{isotopes}
\keyword{isotopes}
