% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_isotope_map.R
\name{build_isotope_map}
\alias{build_isotope_map}
\title{Build Isotope Parent–Daughter Map from Molecular Formula Data}
\usage{
build_isotope_map(mfd)
}
\arguments{
\item{mfd}{A \code{data.table} representing molecular formulas in wide format,
containing isotope count columns (e.g. \code{"12C"}, \code{"13C"}, \code{"14N"}).}
}
\value{
A \code{data.table} with one row per detected element and the columns:
\describe{
\item{element}{Element symbol.}
\item{parent_label}{Most abundant isotope label.}
\item{parent_mass}{Mass number of the parent isotope.}
\item{parent_mf}{Natural mole fraction of the parent isotope.}
\item{daughter_label}{Second most abundant isotope label.}
\item{daughter_mass}{Mass number of the daughter isotope.}
\item{daughter_mf}{Natural mole fraction of the daughter isotope.}
}

If no eligible elements are found, an empty \code{data.table} with the same
column structure is returned.
}
\description{
Internal helper function that constructs an isotope substitution map
for elements present in a molecular formula data table (\code{mfd}).

The function identifies all isotope columns (e.g. \code{"12C"}, \code{"13C"}, \code{"14N"})
contained in \code{mfd}, determines which isotopes are actually present
(atom count > 0 in at least one formula), and then retrieves the two most
abundant stable isotopes per element from the global \code{masses} table.

For each detected element, the most abundant isotope is defined as the
\strong{parent isotope}, and the second most abundant isotope is defined as the
\strong{daughter isotope}. These pairs are later used to generate single
isotope-substituted molecular formulas.
}
\details{
Only elements that occur in \code{mfd} and are represented in \code{masses$label}
are considered. If no isotope columns are detected or none contain
non-zero counts, an empty \code{data.table} is returned.

The function assumes that a global object \code{masses} exists containing at
least the columns:
\itemize{
\item \code{label} – isotope label (e.g. \code{"12C"})
\item \code{symbol} – element symbol (e.g. \code{"C"})
\item \code{mole_fraction} – natural isotopic abundance
}

The resulting isotope map always contains at most two isotopes per element
(parent and daughter), ranked by natural abundance.
}
\seealso{
Other internal functions: 
\code{\link{create_custom_formula_library}()},
\code{\link{extract_aquisition_params}()},
\code{\link{extract_aquisition_params_from_folder}()},
\code{\link{read_xml_peaklist}()},
\code{\link{search_mf_targets}()},
\code{\link{validate_isotope_presence}()}
}
\author{
Boris Koch
}
\concept{internal functions}
\keyword{internal}
\keyword{isotopes}
