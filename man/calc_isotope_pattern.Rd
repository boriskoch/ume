% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_isotope_pattern.R
\name{calc_isotope_pattern}
\alias{calc_isotope_pattern}
\title{Calculate Isotope Pattern}
\usage{
calc_isotope_pattern(
  mf,
  masses = ume::masses,
  threshold = 1e-12,
  rel_threshold = 1e-06,
  max_peaks = 5000L,
  mass_digits = 6L
)
}
\arguments{
\item{mf}{A character vector of molecular formulas or a \code{data.table}
containing isotope count columns.}

\item{masses}{A data.table. Defaults to \code{ume::masses} (based on NIST data)
containing isotope information for elements, including nominal and exact mass,
relative abundance, and Hill system order.}

\item{threshold}{Numeric. Minimum absolute isotope probability retained
during intermediate calculations.}

\item{rel_threshold}{Numeric. Minimum relative abundance retained in the
final isotope pattern.}

\item{max_peaks}{Integer. Maximum number of isotope peaks retained during
intermediate calculations.}

\item{mass_digits}{Integer. Number of decimal places used to merge nearly
identical masses during intermediate calculations.}
}
\value{
A \code{data.table} with one row per isotope peak and the following columns:
\describe{
\item{mf}{Elemental molecular formula.}
\item{mf_iso}{Isotope-specific molecular formula.}
\item{mass}{Exact mass of the isotope composition.}
\item{nominal_mass}{Nominal mass of the isotope composition.}
\item{prob}{Absolute probability of the isotope composition.}
\item{relative_abundance}{Relative abundance normalized to the most
abundant isotope peak.}
\item{isotope_peak}{Peak number ordered by increasing mass.}
}
}
\description{
Calculates the theoretical isotope pattern of a molecular formula based on
natural isotope abundances using multinomial/binomial isotope combinations.
}
\details{
Calculate Theoretical Isotope Pattern

The function calculates all relevant isotope combinations for each element
in a molecular formula and combines them into a theoretical isotope pattern.

For each isotope peak, the function returns the exact mass, nominal mass,
absolute probability, relative abundance, elemental molecular formula (\code{mf}),
and isotope-specific molecular formula (\code{mf_iso}).

The isotope-specific molecular formula uses bracket notation, for example
\verb{[12C2][13C][1H6][16O]}.

Very small isotope peaks can be removed using \code{threshold} and
\code{rel_threshold} to keep the output compact.
}
\examples{
calc_isotope_pattern("C2H6O")
calc_isotope_pattern("FeC10H10", rel_threshold = 1e-4)

}
\seealso{
Other isotopes: 
\code{\link{create_isotope_expanded_table}()},
\code{\link{eval_isotopes}()},
\code{\link{uplot_isotope_precision}()}
}
\concept{isotopes}
\keyword{isotopes}
\keyword{molecular-formula}
