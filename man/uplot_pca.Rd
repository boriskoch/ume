% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uplot_pca.R
\name{uplot_pca}
\alias{uplot_pca}
\title{Plot PCA Results}
\usage{
uplot_pca(
  mfd,
  grp,
  int_col = "norm_int",
  palname = "viridis",
  col_bar = TRUE,
  ...
)
}
\arguments{
\item{mfd}{data.table with molecular formula data as derived from
\code{ume::assign_formulas}. Column names of elements/isotopes must match names in
the \code{isotope} column of \code{ume::masses}; values are integers representing
counts per formula.}

\item{grp}{Character. Name of the column used to define rows/samples in the
PCA matrix.}

\item{int_col}{Character. Name of the intensity column used for PCA
(default = \code{"norm_int"}).}

\item{palname}{Character. Name of the color palette passed to \code{uplot_vk()}
(default = \code{"viridis"}).}

\item{col_bar}{Logical. If \code{TRUE}, show the color bar in the Van Krevelen
plot.}

\item{...}{Additional arguments passed to \code{uplot_vk()}.}
}
\value{
A list containing:
\describe{
\item{pca}{The PCA model object returned by \code{\link[stats:prcomp]{stats::prcomp()}}.}
\item{t_score}{A \code{data.table} with PCA scores for each group.}
\item{fig_vk}{A Van Krevelen plot colored by PC1 loadings.}
\item{fig_pca}{A PCA score plot of PC1 versus PC2.}
\item{mfd}{The input molecular formula data augmented with PC1/PC2 scores
and PC1/PC2 loadings.}
}
}
\description{
Performs Principal Component Analysis (PCA) on molecular formula intensity
data and visualizes the results as a PCA score plot and a Van Krevelen plot
colored by PC1 loadings.
}
\details{
Principal Component Analysis (PCA) Plotting

The PCA is performed on a wide matrix with one row per group defined by
\code{grp} and one column per molecular formula (\code{mf}). Intensities are aggregated
using the mean if multiple values occur for the same combination of \code{grp}
and \code{mf}.

Columns with zero variance are removed before PCA because they cannot be
scaled. The argument \code{grp} defines the observational unit for the PCA, for
example \code{"file_id"}, \code{"sample_id"}, or \code{"ms_id"}.
}
\note{
The function uses \code{\link[stats:prcomp]{stats::prcomp()}} for PCA and \code{\link[=uplot_vk]{uplot_vk()}} for the
Van Krevelen plot.
}
\examples{
res <- uplot_pca(
  mfd = mf_data_demo,
  grp = "file_id",
  int_col = "norm_int"
)

res$fig_pca
res$fig_vk

}
\seealso{
\code{\link[=uplot_vk]{uplot_vk()}}

Other uplots: 
\code{\link{uplot_cluster}()},
\code{\link{uplot_cvm}()},
\code{\link{uplot_dbe_minus_o_freq}()},
\code{\link{uplot_dbe_vs_c}()},
\code{\link{uplot_dbe_vs_ma}()},
\code{\link{uplot_dbe_vs_o}()},
\code{\link{uplot_freq_ma}()},
\code{\link{uplot_freq_vs_ppm}()},
\code{\link{uplot_hc_vs_m}()},
\code{\link{uplot_heteroatoms}()},
\code{\link{uplot_isotope_precision}()},
\code{\link{uplot_kmd}()},
\code{\link{uplot_lcms}()},
\code{\link{uplot_ma_vs_mz}()},
\code{\link{uplot_ms}()},
\code{\link{uplot_n_mf_per_sample}()},
\code{\link{uplot_ratios}()},
\code{\link{uplot_reproducibility}()},
\code{\link{uplot_ri_vs_sample}()},
\code{\link{uplot_vk}()}
}
\concept{uplots}
