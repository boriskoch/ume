% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_neutral_mf.R
\name{check_neutral_mf}
\alias{check_neutral_mf}
\title{Check neutral molecular formulas}
\usage{
check_neutral_mf(mf, masses = ume::masses)
}
\arguments{
\item{mf}{A character vector of molecular formulas.}

\item{masses}{A \code{data.table} containing valid element and isotope
definitions. By default, \code{ume::masses} is used. The table must contain at
least the columns \code{symbol} and \code{label}.}
}
\value{
A \code{data.table} with one row per input entry and the following columns:
\describe{
\item{mf}{Original input string.}
\item{is_empty}{Logical; \code{TRUE} if the input is \code{NA} or empty.}
\item{is_inchikey}{Logical; \code{TRUE} if the input resembles an InChIKey.}
\item{has_charge}{Logical; \code{TRUE} if the formula ends with charge notation
such as \code{+}, \code{-}, \code{+2}, \code{-3}, \verb{2+}, or \verb{3-}.}
\item{is_parseable}{Logical; \code{TRUE} if the string can be fully tokenized
using valid element and isotope labels from \code{masses}.}
\item{is_neutral_mf}{Logical; \code{TRUE} if the input is non-empty, does not
resemble an InChIKey, has no terminal charge notation, and can be fully
parsed as a molecular formula.}
\item{issue}{Character label describing the detected issue. Valid neutral
formulas are labelled \code{"valid_neutral_mf"}.}
}
}
\description{
Checks whether character strings are valid neutral molecular formulas that
can be parsed by \code{convert_molecular_formula_to_data_table()}.

The function is intended as a lightweight pre-check before converting
molecular formulas into element-count tables. It identifies common non-formula
entries such as InChIKeys, charged formulas, empty values, unsupported
isotope notation, and formulas containing unknown element or isotope labels.
}
\details{
Check molecular formulas for neutral formula validity

This function validates syntax only. It does not check chemical plausibility,
valence rules, isotope natural abundance, charge balance, or whether the
molecular formula corresponds to a real compound.

The parser uses the valid element symbols and isotope labels provided in
\code{masses}. This avoids hard-coding element symbols and ensures that the
validation is consistent with \code{convert_molecular_formula_to_data_table()}.

Supported isotope notation follows the convention used in \code{ume}, for example:
\itemize{
\item \verb{[13C]} for one carbon-13 atom
\item \verb{[13C2]} for two carbon-13 atoms
\item \verb{[18O2]} for two oxygen-18 atoms
}

The alternative notation \verb{[13C]2} is currently classified as unsupported
because the isotope count is placed outside the brackets.

Charged formulas such as \code{"C10H13N2+"}, \code{"C11H18N2+2"}, or
\code{"C18H35CaO2Zn+3"} are classified as charged and therefore not neutral.

InChIKeys such as \code{"IOVCWXUNBOPUCH-UHFFFAOYSA-M"} are detected separately
and classified as non-formula identifiers.
}
\examples{
mf <- c(
  "C6H6",
  "C6[13C2]HF15O2",
  "C6[13C]2HF15O2",
  "C4H5FeO4+",
  "C11H18N2+2",
  "IOVCWXUNBOPUCH-UHFFFAOYSA-M",
  NA_character_
)

check_neutral_mf(mf)

valid_mf <- check_neutral_mf(mf)[is_neutral_mf == TRUE, mf]

}
\seealso{
Other molecular formula functions: 
\code{\link{convert_data_table_to_molecular_formulas}()},
\code{\link{convert_molecular_formula_to_data_table}()}
}
\concept{molecular formula functions}
\keyword{chemistry}
\keyword{molecular-formula}
