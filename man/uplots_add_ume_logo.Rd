% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uplots_layout_internal.R
\name{uplots_add_ume_logo}
\alias{uplots_add_ume_logo}
\title{Add the UME package logo to a ggplot}
\usage{
uplots_add_ume_logo(
  p,
  width_mm = 12,
  x_npc_logo = 1.03,
  y_npc_logo = 0.03,
  ...
)
}
\arguments{
\item{p}{A ggplot object to which the UME logo should be added.}

\item{width_mm}{Numeric. Logo width in millimetres.
The height is computed automatically using the PNG's native aspect ratio.}

\item{x_npc_logo}{Numeric in \verb{[0, 1]}. Horizontal position of the logo inside
the plot panel, expressed as NPC (normalized parent coordinates).
\code{1} = right edge, \code{0} = left edge.}

\item{y_npc_logo}{Numeric in \verb{[0, 1]}. Vertical position of the logo inside
the plot panel.
\code{0} = bottom (x-axis), \code{1} = top.}
}
\value{
A modified ggplot object containing the UME logo as an annotation.
}
\description{
Inserts the UME package logo (stored in \code{inst/figures/ume_package_icon.png})
into a ggplot at the bottom-right inside the plotting panel.

The logo is drawn in \strong{absolute device units (mm)} which guarantees:
\itemize{
\item consistent physical size across output devices,
\item correct (non-distorted) aspect ratio,
\item no expansion of axis limits, and
\item predictable placement within the plot.
}

The logo is added via a \code{rasterGrob()} positioned using \strong{panel-relative NPC
coordinates} for reliable placement independent of data scale.
}
\details{
Unlike \code{annotation_raster()} or \code{annotation_custom()} with data units, this
implementation uses a custom \code{rasterGrob()} inside a viewport that specifies
the logo's size in \strong{mm}, not in data space.
This ensures that:
\itemize{
\item the logo's \strong{aspect ratio is preserved},
\item the logo \strong{never distorts} when resizing the plot window,
\item the logo stays correctly positioned relative to the plotting region, and
\item axis limits \strong{remain unchanged}.
}

This approach is robust across PNG/PDF output, facets, different aspect
ratios, and multi-panel arrangements.
}
\keyword{internal}
