% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_molecular_formula_to_data_table.R
\name{convert_molecular_formula_to_data_table}
\alias{convert_molecular_formula_to_data_table}
\title{Convert Molecular Formulas to a Data Table of Element Counts}
\usage{
convert_molecular_formula_to_data_table(
  mf,
  masses = ume::masses,
  table_format = c("wide", "long"),
  keep_mf_old = TRUE,
  isotope_default = c("most_abundant", "lightest"),
  check_neutral = FALSE
)
}
\arguments{
\item{mf}{Character vector of molecular formula(s)
(e.g., \code{c("C10H23NO4", "C10H24N4O2S")}).}

\item{masses}{A data.table. Defaults to \code{ume::masses} (based on NIST data)
containing isotope information for elements, including nominal and exact mass,
relative abundance, and Hill system order.}

\item{table_format}{A string controlling the output format. Either \code{"wide"}
(default) or \code{"long"}.}

\item{keep_mf_old}{Logical. If \code{TRUE} (default), the original input formula is
returned in a column named \code{mf_old}.}

\item{isotope_default}{A string defining which isotope should be used when
an element is given without explicit isotope notation. Either
\code{"most_abundant"} (default) or \code{"lightest"}.}

\item{check_neutral}{Logical. If \code{TRUE} (default = \code{FALSE}), input formulas
are checked with \code{\link[=check_neutral_mf]{check_neutral_mf()}} before parsing.}
}
\value{
A \code{data.table} in wide or long format.
}
\description{
Parses molecular formulas and returns a \code{data.table} where each row
represents one molecular formula and each element or isotope is represented
by a separate count column.
}
\details{
The function supports normal element notation such as \code{C6H12O6} and bracketed
isotope notation such as \verb{[13C]}, \verb{[13C2]}, and \verb{[18O2]}.

Input formulas are parsed using the element symbols and isotope labels
provided in \code{masses}. This avoids hard-coded element lists and allows rare
elements to be parsed as long as they are present in \code{masses}.

By default, input formulas are checked with \code{\link[=check_neutral_mf]{check_neutral_mf()}} before
parsing.

The standardized molecular formula \code{mf} is generated using dynamic Hill
ordering:
\itemize{
\item if carbon is present: \code{C}, then \code{H}, then all other elements alphabetically
\item if carbon is absent: all elements alphabetically, including \code{H}
}
}
\seealso{
Other molecular formula functions: 
\code{\link{check_neutral_mf}()},
\code{\link{convert_data_table_to_molecular_formulas}()}
}
\concept{molecular formula functions}
\keyword{chemistry}
\keyword{molecular-formula}
